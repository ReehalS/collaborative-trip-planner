model Trip {
  id          String            @id @default(uuid())
  userIds     String[]
  joinCode    String            @unique
  activities  ActivityToTrip[]
  country     String
  city        String?
  latitude    Float
  longitude   Float
  timezone    String
  users       UserToTrip[]
}